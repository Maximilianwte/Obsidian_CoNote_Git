{
  "name": "conote-api",
  "version": "1.0.0",
  "description": "Cloud Function: GCS proxy + share management for Conote",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && gcloud functions deploy conote-api --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point app --source dist --set-env-vars GCS_BUCKET=obsidian-conote"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "@google-cloud/storage": "^7.7.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
